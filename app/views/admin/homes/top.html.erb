<div class="container">
  <div class="row">
    <h2>注文履歴一覧</h2>
    <table class="table table-hover table-inverse">
      <thead>
        <tr>
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
        </tr>
      </thead>
      <tbody>
         <% @orders.each do |order| %>
        <tr>
<<<<<<< HEAD
            <td>
              <%= link_to admin_order_path(order) do %>
                <%= order.created_at %>
              <% end %>
            </td>
            <td><%= order.member.last_name %></td>
            <td>
              <% @sum = 0  %>
              <% order.order_products.each do |order_product| %>
              <% @sum += order_product.quantity %>
              <% end %>
              <%= @sum %>
            </td>
=======
          <% @orders.each do |order| %>
            <td>
              <u><%= link_to order.created_at, admin_order_path(order.id),class: "text-dark" %></u>
            </td>
            <td><%= order.member.last_name %><%= order.member.first_name %></td>
            <td><%= order.order_products.quantitiy %></td>
>>>>>>> 32d23ff5dae014545a443ba5cb87862af1d81ea0
            <td><%= order.status %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <%= paginate @orders %>
  </div>
</div>
